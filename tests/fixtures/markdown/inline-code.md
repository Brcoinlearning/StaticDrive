Use the `console.log()` function to print debug information.

The `Array.prototype.map()` method creates a new array.

When in doubt, run `npm install` and try again.

The variable `count` holds the current value.

Call `api.fetchData({ id: 42 })` to retrieve the record.

In Python, `print("hello world")` is the simplest program.
